@model LibreriaWeb.Models.Atletas.AtletaDisciplinaIdViewModel

@{
    ViewData["Title"] = "ListaAteltasXDisciplina";
}

<h1>Buscar Atleta por Id de Disciplina</h1>

<h4>Filtrar</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="ListaAteltasXDisciplina">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="DisciplinaId" class="control-label" ></label>
                <input asp-for="DisciplinaId" class="form-control" required />
                <span asp-validation-for="DisciplinaId" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Buscar" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>
@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
